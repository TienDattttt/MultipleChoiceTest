
spring:
  application:
    name: exam-service
  datasource:
    url: jdbc:sqlserver://DESKTOP-ISSK39T;databaseName=ExamDB;encrypt=false;trustServerCertificate=true
    username: sa
    password: 1224454
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true
    open-in-view: false

  mvc:
    problemdetails:
      enabled: true

server:
  port: 8080

